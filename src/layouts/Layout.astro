---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{title} | i build stuff, sometimes</title>
    <meta
      name="description"
      content="Stuti Mishra - Software Developer who enjoys building full-stack web applications and working with data."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="canonical" href="https://stutiwhy.netlify.app/" />

    <link
      rel="preload"
      href="/fonts/montserrat-latin-wght-normal.woff2"
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />

    <meta
      property="og:title"
      content="Stuti Mishra | Software Developer"
    />
    <meta
      property="og:description"
      content="Portfolio of Stuti Mishra, a Software Developer who enjoys building full-stack web applications and working with data."
    />
    <meta
      property="og:image"
      content="https://stutiwhy.netlify.app/og.image.png"
    />
    <meta property="og:url" content="https://stutiwhy.netlify.app" />
    <meta property="og:type" content="website" />
    <meta
      property="og:site_name"
      content="Stuti Mishra Portfolio"
    />

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Stuti Mishra",
        "alternateName": "Stuti Rajkumar Mishra",
        "url": "https://stutiwhy.netlify.app",
        "image": "https://stutiwhy.netlify.app/og.image.png",
        "sameAs": [
          "https://github.com/stutiwhy",
          "https://linkedin.com/in/stutiwhy"
        ],
        "jobTitle": "Software Developer",
        "description": "Software Developer crafting robust backends and interactive full-stack applications. Turning complex logic into seamless digital experiences."
      }
    </script>
  </head>
  <body class="bg-[--background] sm:px-28 lg:px-20 px-9">
    <slot />
  </body>
</html>

<style is:global>
  @font-face {
    font-family: "Montserrat Variable";
    src: url("/fonts/montserrat-latin-wght-normal.woff2")
      format("woff2-variations");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  :root {
    --background: #101010;
    --sec: #6cffb3;
    --white: #dfdfdf;
    --white-icon: #f3f3f398;
    --white-icon-tr: #f3f3f310;
  }

  * {
    font-family:
      "Montserrat Variable",
      -apple-system,
      system-ui,
      sans-serif;
    box-sizing: border-box;
    padding: 0;
    margin: 0;
  }

  *::selection {
    background-color: var(--sec);
    color: var(--background);
  }

  /* Scrollbar styles */

  ::-webkit-scrollbar {
    width: 15px;
  }

  ::-webkit-scrollbar-track {
    background: var(--container);
    border-radius: 30px;
  }

  ::-webkit-scrollbar-thumb {
    background: var(--background);
    border-radius: 10px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: var(--pink);
  }

  /* Scrollbar styles for Firefox */
  * {
    scrollbar-width: thin;
    scrollbar-color: var(--line) var(--container);
  }
</style>
